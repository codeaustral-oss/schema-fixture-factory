# Contributing

Good contributions include support for more JSON Schema keywords, better format generators, and fixture presets for common API documentation needs.

```bash
npm install
npm test
```

Please keep the generator deterministic for a given seed.
